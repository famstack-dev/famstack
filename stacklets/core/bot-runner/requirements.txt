matrix-nio[e2e]>=0.24,<1.0
aiohttp>=3.9,<4.0
loguru>=0.7,<1.0
Pillow>=10.0,<12.0
PyYAML>=6.0,<7.0
# Parse YAML frontmatter from markdown wiki pages (memory stacklet).
python-frontmatter>=1.0,<2.0
markdown>=3.5,<4.0
pypdf>=4.0,<6.0
# Render scanned PDFs to PNG for vision-capable classifiers. Pure-Python
# wheel, no system deps. See stacklets/docs/bot/pdf_render.py.
pypdfium2>=4.0,<6.0
# Extract the main body of a web page and render it as Markdown.
# Used by the archivist's URL-capture pipeline — pasted links to
# articles get summarized + filed without going through Paperless.
trafilatura>=1.12,<3.0
